sudo: required
language: generic 
services:
  - docker

before_install:
  - docker build -t matteofiameni/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true matteofiameni/docker-react npm run test